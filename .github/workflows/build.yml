name: Build and Release

on:
  push:
    branches:
      - main
    tags:
      - '*'
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  phpcs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'

      - name: Install dependencies
        run: composer install

      - name: Run PHPCS
        run: vendor/bin/phpcs

  eslint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Run ESLint
        run: bun run lint-js

  phpunit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'

      - name: Install dependencies
        run: composer install

      - name: Run PHPUnit
        run: vendor/bin/phpunit

  jest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Run Jest
        run: bun run test-unit-js -- --ci

  build:
    runs-on: ubuntu-latest
    needs: [phpcs, eslint, phpunit, jest]
    if: github.event_name == 'push' && contains(github.ref, 'refs/tags/')
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Build
        run: bun run build

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'

      - name: Composer Install
        run: composer install --no-dev --no-suggest --optimize-autoloader --no-interaction

      - name: Set Version in PHP File
        run: |
          sed -i "s/Version:.*$/Version: ${{ github.ref_name }}/" soccr.php

      - name: Set Stable tag in readme.txt (stable releases only)
        if: "!contains(github.ref_name, '-')"
        run: |
          sed -i "s/Stable tag:.*$/Stable tag: ${{ github.ref_name }}/" readme.txt

      - name: zip
        run: bun run plugin-zip

      - name: Copy zip with version suffix
        run: cp soccr-reloaded.zip soccr-reloaded-${{ github.ref_name }}.zip

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build
          path: |
            soccr-reloaded.zip
            soccr-reloaded-${{ github.ref_name }}.zip

  release:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          body: |
            Release ${{ github.ref }}

      - name: Download build
        uses: actions/download-artifact@v4
        with:
          name: build

      - name: Upload Release Asset (versioned)
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./soccr-reloaded-${{ github.ref_name }}.zip
          asset_name: soccr-reloaded-${{ github.ref_name }}.zip
          asset_content_type: application/zip

      - name: Upload Release Asset (without version)
        id: upload-release-asset-without-version
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./soccr-reloaded.zip
          asset_name: soccr-reloaded.zip
          asset_content_type: application/zip

  wporg-assets-trunk:
    name: Push to trunk
    runs-on: ubuntu-latest
    needs: release
    if: "!contains(github.ref_name, '-')"
    steps:
      - uses: actions/checkout@v4
      - name: Update Stable tag
        run: |
          sed -i "s/Stable tag:.*$/Stable tag: ${{ github.ref_name }}/" readme.txt
      - name: WordPress.org plugin asset/readme update
        uses: 10up/action-wordpress-plugin-asset-update@stable
        env:
          SVN_PASSWORD: ${{ secrets.SVN_PASSWORD }}
          SVN_USERNAME: ${{ secrets.SVN_USERNAME }}
          SLUG: soccr-reloaded

  wporg-deploy:
    name: wordpress.org Deploy
    needs: wporg-assets-trunk
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set version and stable tag
        run: |
          sed -i "s/Version:.*$/Version: ${{ github.ref_name }}/" soccr.php
          sed -i "s/Stable tag:.*$/Stable tag: ${{ github.ref_name }}/" readme.txt
        shell: bash
      - name: WordPress Plugin Deploy
        uses: 10up/action-wordpress-plugin-deploy@stable
        env:
          SVN_PASSWORD: ${{ secrets.SVN_PASSWORD }}
          SVN_USERNAME: ${{ secrets.SVN_USERNAME }}
          SLUG: soccr-reloaded
